export const MARGIN_RATIO = 1 / 10
export const ATOM_RATIO = 1 / 3

export const OUTER_RATIO = MARGIN_RATIO * 2
export const INNER_RATIO = 1 - OUTER_RATIO
export const INNER_ATOM_RATIO = INNER_RATIO * ATOM_RATIO
export const TRIANGLE_RATIO = Math.sqrt(3 / 4)

export const UNIT = 10
export const HALF_UNIT = UNIT / 2
export const TRIANGLE_UNIT = UNIT * TRIANGLE_RATIO
export const TRIANGLE_OFFSET = UNIT - TRIANGLE_UNIT
export const MARGIN_UNIT = UNIT * MARGIN_RATIO
export const ATOM_UNIT = UNIT * ATOM_RATIO
export const OUTER_UNIT = UNIT * OUTER_RATIO
export const INNER_UNIT = UNIT * INNER_RATIO
export const INNER_ATOM_UNIT = UNIT * INNER_ATOM_RATIO

export const MAGNET_UNIT = 10
